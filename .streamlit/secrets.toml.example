# Streamlit Cloud main file: app.py
# https://intelligentccms.streamlit.app/
#
# Same Bootstrap HTML UI as `python app.py` — FastAPI is mounted at /backend
# inside Streamlit (no separate Render URL required).
#
# Optional: set BACKEND_API_URL only if the API is hosted elsewhere.

MONGO_URI = "mongodb+srv://user:pass@cluster.mongodb.net/"
DB_NAME = "hackathon"
GEMINI_API_KEY = "your-gemini-key"
GOOGLE_MAP_API_KEY = "your-maps-key"
